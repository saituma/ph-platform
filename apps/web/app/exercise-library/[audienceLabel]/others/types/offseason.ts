import type { OtherSectionConfig } from "../types";

export const offseasonSection: OtherSectionConfig = {
  type: "offseason",
  label: "Off-Season Program",
  summary: "Off-season will get its own concept later, so it now has an isolated config file.",
  concept: "content",
};
