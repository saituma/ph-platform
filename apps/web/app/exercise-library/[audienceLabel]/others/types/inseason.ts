import type { OtherSectionConfig } from "../types";

export const inseasonSection: OtherSectionConfig = {
  type: "inseason",
  label: "In-Season Program",
  summary: "This section will branch into age groups and weekly scheduling, so it has its own dedicated config.",
  concept: "age-schedule",
};
