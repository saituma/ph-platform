# These will be automatically created by Neon Launchpad (https://neon.new).
# You will also need to 
DATABASE_URL=
DATABASE_URL_POOLER=

# Cloudflare Worker (Better Auth). TanStack `/api/auth/*` proxies here.
BETTER_AUTH_URL=
# Optional: same value for Vite client if you call the Worker directly (no proxy).
# VITE_BETTER_AUTH_URL=
